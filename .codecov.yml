coverage:
    precision: 5
    round: down
    range: "70...100"

    status:
        project:
            target: auto
            if_no_uploads: error

        patch:
            target: "80%"
            if_no_uploads: error

comment:
    layout: "header, diff, changes, suggestions"
    behavior: default
